# brewfile.global – für systemweite (globale) Tools

# Casks (werden systemweit nach /Applications installiert)
cask "bentobox"      # Fenster-Management für macOS
cask "google-chrome"
cask "microsoft-edge"
cask "whatsapp"
cask "logi-options-plus"

# CLI-Tools, systemweit
brew "git"
brew "tree"
